UI_FREEZE.md

UIは変更禁止（見た目・配置・文言・サイズ・余白・色・ボタン数）

HTML構造を変えない（要素の追加/削除/入れ替え禁止）

CSSの変更禁止（クラス名、レイアウト、フォント倍率、余白、色）

修正は 内部実装のみ（JS/GASのロジック、通信、エラー処理）

出力は 必ず“完全な1枚ファイル”（途中差分や断片は禁止）

もしUI変更案がある場合：提案として文章で書く（実装しない）

受け入れ条件：UIスクショと同一に見えること
