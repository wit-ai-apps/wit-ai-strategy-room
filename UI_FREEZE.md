UI_FREEZE（確定版・運用用）

UIは一切変更禁止
（見た目／配置／文言／サイズ／余白／色／ボタン数を含む）

HTML構造を変更しない
（要素の追加・削除・入れ替えはすべて禁止）

CSSの変更禁止
（クラス名／レイアウト／フォント倍率／余白／色を含む）

修正は内部実装のみ許可
（JavaScript / GAS のロジック、通信、エラー処理に限定）

出力は必ず「完全な1枚ファイル」
（差分、部分コード、断片提示は禁止）

UI変更案がある場合
→ 実装せず、提案として文章でのみ記述

受け入れ条件
→ UIスクリーンショットと視覚的に完全一致していること
