1) Rex / Gemini に渡す“冒頭固定フレーズ”（毎回これ）
貼る場所：Issue本文の先頭 or チャット冒頭

テンプレ（短い版）

最初に GitHub の UI_FREEZE.md を読んでください。
UI（見た目・配置・文言・サイズ・余白・色・ボタン数）は一切変更禁止。
修正は内部実装（JS/GASロジック、通信、エラー処理）のみ。
出力は必ず「完全な1枚ファイル」。差分・断片は禁止。
まず「UI_FREEZEを読み、遵守する」と宣言してから作業を開始してください。


テンプレ（強い版：UI崩れ再発防止）

【最優先】UI_FREEZE.md を読み、内容を厳守してください。
UI/HTML構造/CSSは一切変更禁止（要素追加・削除・入替、クラス名変更も禁止）。
内部実装のみで修正し、出力は必ず完全な1枚ファイルで提出。
受け入れ条件：UIスクショと視覚的に完全一致。
これに反する提案・実装は差し戻し。まず「遵守宣言」を返信してください。

2) 作業依頼テンプレ（その下に続けて書く“依頼本文の型”）

これを貼ると、Rex/Geminiが勝手に話を広げにくくなる。

【対象】（例：AI戦略会議室 / Code.js / index.html）
【やりたいこと】（1行）
【困っている症状】（再現手順を短く）
【理想の状態】（受け入れ条件）
【禁止】UI変更・HTML構造変更・CSS変更・分割納品
【提出物】単一ファイル（どのファイルか明記）
【検証】A→C→B の順で行う（必要なら項目を書く）

3) UI_FREEZE違反時の“即差し戻し文”（感情抜きで強い）

相手がAIでも人でも、これを貼れば一撃で戻せる。

差し戻しテンプレ

UI_FREEZE違反のため差し戻しです。
UI（見た目/配置/文言/サイズ/余白/色/ボタン数）または HTML構造 または CSS に変更が入っています。
UI_FREEZE.md の規定どおり、内部実装（JS/GASロジック、通信、エラー処理）のみで修正してください。
出力は必ず完全な1枚ファイル。差分・断片の提出は禁止です。
次の返信は「違反箇所の自己申告（何を変えたか）」→「UIを一切変えずにやり直した完全ファイル提出」でお願いします。

4) 受け入れチェック（あなたの判断が速くなる“3点だけ”）

レビュー時はこれだけ見ればOK。

UIがスクショと同一に見えるか（最重要）

HTML要素の増減・入替が無いか（DOMを触ってないか）

CSS（クラス名・レイアウト・色・余白）が変わってないか
